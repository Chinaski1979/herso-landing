---
// Blog tags temporarily disabled — restore when blog is re-enabled.
return Astro.redirect("/");
---

<!-- export const prerender = true;
import { getCollection } from "astro:content";
import type { AstroSeoProps } from "@astrolib/seo";
import TagComponent from "@components/blog/Tag.astro";
import BaseLayout from "@layouts/BaseLayout.astro";

export async function getStaticPaths() {
	const allPosts = (await getCollection("posts")).filter((post) => {
		const [postLang] = post.slug.split("/");
		return postLang === "en";
	});

	const uniqueTags = [...new Set(allPosts.flatMap((post) => post.data.tags))];
	return uniqueTags.map((tag) => {
		const filteredPosts = allPosts
			.filter((post) => post.data.tags.includes(tag))
			.map((post) => {
				const [, ...slugParts] = post.slug.split("/");
				return { ...post, slug: slugParts.join("/") };
			});
		return {
			params: { tag },
			props: { posts: filteredPosts },
		};
	});
}

const { tag } = Astro.params;
const { posts } = Astro.props;

const seo: AstroSeoProps = {
	title: `${tag} | Hermosa Software`,
	description: `Articles tagged '${tag}' on the Hermosa Software blog.`,
	canonical: `https://hermosasoftware.io/tags/${tag}/`,
};
---

<BaseLayout seo={seo}>
	<TagComponent tag={tag} posts={posts} />
</BaseLayout> -->
